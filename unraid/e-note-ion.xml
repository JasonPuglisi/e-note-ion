<?xml version="1.0"?>
<Container version="2">
  <Name>e-note-ion</Name>
  <Repository>ghcr.io/jasonpuglisi/e-note-ion</Repository>
  <Registry>https://github.com/JasonPuglisi/e-note-ion/pkgs/container/e-note-ion</Registry>
  <Network>bridge</Network>
  <Privileged>false</Privileged>
  <Support>https://github.com/JasonPuglisi/e-note-ion/issues</Support>
  <Project>https://github.com/JasonPuglisi/e-note-ion</Project>
  <Overview>E•NOTE•ION is a cron-based content scheduler for Vestaboard split-flap displays. Supports both the Note (3×15) and Flagship (6×22). Content is defined in JSON files and displayed on a schedule with priority queueing. Ships with bundled contrib content (opt-in via CONTENT_ENABLED) and supports personal content via a host-mounted directory.</Overview>
  <Category>HomeAutomation</Category>
  <TemplateURL>https://raw.githubusercontent.com/JasonPuglisi/e-note-ion/main/unraid/e-note-ion.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/JasonPuglisi/e-note-ion/main/assets/icon.png</Icon>

  <Config
    Name="Config file"
    Target="/app/config.toml"
    Default="/mnt/user/appdata/e-note-ion/config.toml"
    Mode="rw"
    Description="Required. Path to your config.toml on the host. Copy config.example.toml from the repo, fill in your API keys and settings, and point this path at the result. Display model (note/flagship), public mode, and enabled contrib content are all set in config.toml under [scheduler]."
    Type="Path"
    Display="always"
    Required="true"
    Mask="false"/>

  <Config
    Name="User content directory"
    Target="/app/content/user"
    Default="/mnt/user/appdata/e-note-ion/content"
    Mode="rw"
    Description="Optional. Path to a directory of personal content JSON files on the host. Files placed here are always loaded automatically. Leave blank if you only want to use bundled contrib content."
    Type="Path"
    Display="always"
    Required="false"
    Mask="false"/>

  <Config
    Name="Timezone"
    Target="TZ"
    Default="America/Los_Angeles"
    Mode=""
    Description="Container timezone (e.g. America/New_York, Europe/London). Controls when cron jobs fire. Should match the timezone set in config.toml under [scheduler].timezone, or be the only timezone setting if you omit that config key."
    Type="Variable"
    Display="always"
    Required="false"
    Mask="false"/>

  <Config
    Name="Webhook port"
    Target="8080"
    Default="32800"
    Mode="tcp"
    Description="Optional. Host port for the HTTP webhook listener (used by Plex and other push integrations). Only takes effect if [webhook] is enabled in config.toml with bind = &quot;0.0.0.0&quot;. The container always listens on 8080 internally; this maps it to a host port. 32800 is the recommended default to avoid conflicts with other common services on 8080."
    Type="Port"
    Display="advanced"
    Required="false"
    Mask="false"/>
</Container>
